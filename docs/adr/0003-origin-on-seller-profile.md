# Ship-from address lives on the seller profile

The template stores origin on the listing pickup address. We put Origin on the seller profile instead, so one street address serves all listings, search can stay coarse, and the public listing does not need the full street. Listing location can still serve maps and local pickup.
